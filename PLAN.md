agama-release-checker gathers enough data for now, let's work on presenting it better.

OBS Requests Report:

We have a --recent-rq CLI option. It turns out I want to do BOTH queries,
regular and recent, and combine their result in their output. Then remove the
CLI option.
